<?php
//add_action( 'woocommerce_before_main_content', 'before_the_shop', 10 );
function before_the_shop() {
	/*
	//if ( is_front_page() ):
	$object = get_queried_object();
	global $post;
	_log( $post );

	$args      = [
		'p'         => $object->ID,
		'post_type' => 'any'
	];
	$the_query = new WP_Query( $args );
	while ( $the_query->have_posts() ) : $the_query->the_post();


	endwhile;
	wp_reset_postdata();
	*/
	// endif;
}

